/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/ims',      // dairy sacco graders portal tells Next.js this app lives under /dairy
    assetPrefix: '/ims',   // optional: ensures static assets load correctly
    reactStrictMode: true
  };
  
  module.exports = nextConfig;
  
  