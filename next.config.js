/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/crmv2',      // dairy sacco graders portal tells Next.js this app lives under /dairy
    assetPrefix: '/crmv2',   // optional: ensures static assets load correctly
    reactStrictMode: true
  };
  
  module.exports = nextConfig;
  
  