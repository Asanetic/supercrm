/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/erp',      // dairy sacco graders portal tells Next.js this app lives under /dairy
    assetPrefix: '/erp',   // optional: ensures static assets load correctly
    reactStrictMode: true
  };
  
  module.exports = nextConfig;
  
  