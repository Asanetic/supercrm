/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/crm',      // dairy sacco graders portal tells Next.js this app lives under /dairy
    assetPrefix: '/crm',   // optional: ensures static assets load correctly
    reactStrictMode: true
  };
  
  module.exports = nextConfig;
  
  